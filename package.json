{
  "name": "creativefaces-site",
  "private": true,
  "description": "Static site for CreativeFaces Salon and Makeup Bar",
  "scripts": {
    "build": "tailwindcss -c tailwind.config.cjs -i src/input.css -o tailwind.css --minify",
    "watch": "tailwindcss -c tailwind.config.cjs -i src/input.css -o tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  }
}
